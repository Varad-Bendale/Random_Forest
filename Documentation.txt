So here we are going to make a descion tree algorithm from scratch in c++ . 
As first matter of action let me give you how the descion trees work right now 
So we have the dataset we look for the parameter known as gini impurity which 
tells us how well the parameter which we are taking into account is dividing the data 
Gini impurity is given by 1- ( probs of the yes ) ^2 - ( probs of the no ) ^2
Now see here we are subtracting both the probabilities because of normalisation
part see lesser the gini gets its more better for us to understand if just take the 
Squares of probabilities  we may never know what amount of thing is correct and what is not that way 
And then we need to normalise again with the two things we split  the data for that we use 
As we know this lets move forwards 

Okay before going to anywhere let me just tell you how am I processing the data 
See suppose the data is like 

col-1 col-2 col-3 col-4    target 

So now what we do is here is we are first of all storing all the initial positions of
all the data points before all the sorting the positions we are storing those as we don't 
want to sort the process again and again then we keep is the real data which is actually there
like the index represents the data that one and the next one is the 
Target like what is the data representing that one I am speaking of 
And then we just sort out all the dataset so then the greater the thing is we can identify 
that and if smaller we can identify that way both the things in the proper manner as we sort it out 
We are also storing all the targets which are unique we store them in the global vector target 
things and we use it everywhere as well better for the thing also why not just take the set see 
its kind of personal thing don't like auto iterators and set requires auto iteraters only and the 
index iteration they dont really support so yeah had to use the set and then transfer it to the vector 
Now see the best manner to make the dataset is just move the dataset by 90 degrees and then sort it out and then do it or operate on it 

Okay as we come to the descion tree main function we are making two of the tree data stuture one
of them is to store all the threshold like the variable which needs to be seen for the the best
gini impurity and the next one which is the index stores which column to look for the finding 
the best input which is there 
From here we call the function rec ( short form for recursion ) and this is majorly responsible 
to call out all the function which would give us the results and build the entire tree accordingly
by calling all the recursions 
From here the perfect variable is called so let me just tell you about it and then ill come again
on this function lets just backtrack this thing 
Now in the perfect variable it is responsible for making all the proper data ready for the next 
procedures there are two main things the pr which is is the main variable and the target variable 
for the same so this stores out this and then on the the next one is like it takes the mid of the 
both the 2 -2 columns and then store it so its like a criteria thing if higher than this then this 
if lower than this then this and then stores the index or the position of the same 
And then we call the function which looks for the gini impurity we get the correct impurity and 
then we just give out all the info which is needed for the thing 
gini_impurity 
num
variable_pos  the position of which column it is 
thresh_data_loc  the position of the number in the columns 
gini_left
gini_right
Also while putting the numbers in the pr we need to check the if the thing is used in the earlier
the tree it could make the tree useless if the same is used again and again so we keep the condition for it 

Now here we are going though all the rows  and all the columns to check all the gini which is the 
best fit here we get is 
Okay now we come on the very main part of the thing of there function get perfect now we just go in
the thing we have the column ready we just go through one by one in the pos we stored which was the
middle of the 2 things and then count the targets from there like till the threshold how many are 
there and which of them is giving us the best gini impurity we do like that way and then get the 
answer for it we go iterating from one by one here in the column so initially the number of targets
are full in the right part and nil in zero part but as the position shifts one by one our count 
increases in the left part and decreases in the right part 


Moving forward we had the discusion about the gini impurity so now lets move towards building that 
one see here we have the two thing now first gini_impurity_subtree checks 
Out for the the gini impurity of the each of the subtree or the columns which we break out so all 
the left one and the right one needs to be computed separatly so this functions is used here and 
the gini impurity is calculated we here are storing 5 things 
Gini impurity 
Left tree gini 
Right tree gini 
Largest left tree number 
Largest right tree number 
Now we are using the buffer over here why so cuase see this function is just called insane amount 
of time and calling all the malloc and the freeing os the vector takes 
A lot of time and we need optimisation so we just make a buffer a global variable and we are making
almost 15 of those buffers why so would be explained in next few parts 
We just make the buffer of the 5 point vector and then reuse it every single time for the iteration 
Now see we kinda of put almost all the variables used in the buffer and then use it and just add the 
local thread so the race condition s can be avoided and we only keep the target things as a normal one 
cause its just there for the reference and its not changed further thats why a global variable it is 

Okay after all of these we get the all the parts we need about all perfect variable in there dataset 
which is giving the least amount of the gini impurity 
Well lets get back to the rec function as I kinda had the break the flow for the few of the functions
which were called in here 
Okay so the next function used in here is the new dataset listen we splitted the data and then now we 
need the splitted data to work again to make the new of the data which 
Is been spliced right so now we do is we go in the recursion and then as the dataset is sorted its works
perfectly fine for the splitting and then we pass the upside data and then the downside data and then our 
tree goes again 
Returing back to the rec the recursion continues and we do it that way 



There is this function known as function its job is to make the finishing blow and iteration of all the
tree and also its have the to give out the answer like whatever the answer is comming out from the tree 
it gives us that it has only that job also we have a thing fixed here like if the result value is greater 
than the tree value we go left and vice a versa we go right 

And the function lead is there this is used for just making the whole tree and adding all the leafs and 
declaring them leafs like when to stop at time of the iteration this is the one responsible for it 
See its like a safety net rec handles all the leafs definition and all of those earlier it was like rec 
just does the stuff it needs to do and then leaf would handle all the leaves making part but it kinda 
was failing so right now rec is made so that it handles and defines all the leafs and then leaf function 
is just kept for the safety if anything is missing or left out it catches it like that way 
See if you see in the rec there are few conditions where like if the gini is low just return out but there
needs a leaf na over there a node which tells whats in majority leaf was failing right over here so it's
just better to add those conditions in rec itself 
Okay now there was a bug which was happening in the earlier forms of the code which was it was going in 
recursive conditions again and again and the reason was I was just checking out the Hashmap but the infinite
recursion was not been corrected on now just for imagination think like 5->(3,7) and 3->(2,5) it goes in 
recursion now there are few of the options like add the level of the tree but it breaks also there is to 
directly remove the tree value from the Hashmat but it is problematic if we go backtracking we dont find 
the thing so its better to just add the flags to these nodes so there is a new Hashmat added to this part
now also this would be of the node itself cause the thing is same number can occur but the map is meant for 
the tree node only so yeah need a set over here as well cuase unordered map dont support custom data stuctures  




Okay these things were about the descion trees but lets jump on the random forest now see now the very first thing which we do here is we need to randomly select the rows and the 
columns which need to be considered for the tree mind it duplicated rows are kinda of allowed in here as they benefit the forest to get the answer better also the number of columns 
choosing we have them inside a range from the square root of the number of columns to the number of columns why because way too less may just give us the worst tree which is just no 
good so we keep that limits 

Now one of the most painful mistake which I was creating I thought I might just mention it see in the function leaf the issue occurring was I was thinking that the two flags leaf 
status and right status are way more important cause it is needed for the later on part for finding out answers well silly me I was just thinking on the part that it must be like 
that it needs to way more correct but also I had a thought process where like 
Suppose a leaf is been created and there is a stomp in the right or vice a versa then if the stomp is given the leaf symbol it may just break out very bad and the tree may never 
continue but see I kinda imagined it wrong the flow goes like 
Let me give you an example instead of plain explanation 

leaf_info = {
    5.0 → { left answer: 0.3,  right answer: 0.7 }
    3.2 → { left answer: 0.1,  right answer: 0.9 }
}

    [5.0]			[5.0]
   /     \        ->   	/    \
 null    null		    [0.3]   [0.7]

See right now we splitted a stomp at this point of time they are leafs from the perspective of the compiler as it doesn't knows that it goes further so we need to stop it at the 
point here and declare them as a leafs 

A scenario where we don't treat them as a child 
 
	 [5.0]
	/    \
     [3.0]  [0.7]
		   \
		  [0.7] 
			\
			[0.7] 


And it goes on continuation as we never just break out the loop 
So simply this is the thing with the help if the set which we have we keep track of what things are already used so that  infinite recursion is also stopped and and this type of 
recursion is also stopped see we dont care about these flags its just to break out of the part which is been running constantly simple 

And as we have already of the trees on which reference we are working so it doesn't matter a lot 
I thought I might share it as it really blocked the way like hell 


